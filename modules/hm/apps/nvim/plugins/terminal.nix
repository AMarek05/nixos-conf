{
  programs.nvf.settings.vim = {
    terminal.toggleterm = {
      enable = true;

      mappings.open = "<C-\\>";

      setupOpts = {
        direction = "float";
        size = 10;
      };
    };
  };
}

