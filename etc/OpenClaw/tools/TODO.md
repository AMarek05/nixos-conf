# TODO — gh tool fix

- [x] Update gh.sh to support both `--flag=value` and `--flag value` formats
- [x] Add `--state` argument to gh.nix
- [x] Update examples to use `--title=` format (avoids OpenClaw parser splitting spaces)
- [ ] Rebuild NixOS to deploy updated tool
- [ ] Test with actual PR creation
